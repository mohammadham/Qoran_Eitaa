{
  "name": "telegram-content-bot",
  "version": "1.0.0",
  "description": "A content management bot for Telegram on Cloudflare Workers.",
  "main": "index.js",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev src/index.js --test-runner vitest"
  },
  "dependencies": {
    "itty-router": "^4.0.23"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "wrangler": "^3.57.0"
  }
}
